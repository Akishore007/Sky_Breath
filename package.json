{
  "name": "skybreath-root",
  "version": "1.0.0",
  "description": "SkyBreath - Real-time air quality monitoring and health recommendations",
  "scripts": {
    "dev": "node scripts/start-all.js",
    "dev:all": "node scripts/start-all.js",
    "dev:frontend": "cd app && npm run dev:frontend",
    "dev:backend": "cd backend && python manage.py runserver",
    "build": "cd app && npm run build",
    "install:all": "npm install && cd app && npm install && cd ../backend && python -m venv venv && venv\\Scripts\\python -m pip install -r requirements.txt"
  },
  "keywords": [
    "air-quality",
    "weather",
    "health",
    "monitoring"
  ],
  "author": "SkyBreath Team",
  "license": "MIT",
  "devDependencies": {
    "open": "^11.0.0"
  }
}
